spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:guest_db}
    username: ${POSTGRES_USER:hrms}
    password: ${POSTGRES_PASSWORD:hrms}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect